функция пример1(?аргумент1, ?аргумент2) {
    свойство => ?переменная2;
    если (?переменная2 == свойство) {
        печатать(?переменная2);
        печатать(?аргумент1);
        продолжить;
    };
};



